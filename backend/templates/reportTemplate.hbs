<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <style>
    @page {
      margin: 20mm 15mm;
    }
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      font-size: 14px;
      color: #111;
    }
    .title-page {
      text-align: center;
      margin-top: 200px;
      page-break-after: always;
    }
    .title-page h1 {
      font-size: 28px;
      margin-bottom: 20px;
    }
    .title-page p {
      font-size: 16px;
    }
    h2.section-title {
      font-size: 16px;
      margin-top: 40px;
      border-bottom: 1px solid #ccc;
      padding-bottom: 5px;
    }
    pre.section-content {
      white-space: pre-wrap;
      font-family: inherit;
      margin-top: 8px;
    }
    footer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      text-align: center;
      font-size: 12px;
      color: #888;
    }
  </style>
</head>
<body>
  <div class="title-page">
    <h1>Rapport Thérapeutique</h1>
    <p>Patient : {{patientName}}</p>
    <p>Date de naissance : {{birthDate}}</p>
    <p>Date du rapport : {{reportDate}}</p>
    <p>Ergothérapeute : {{therapistName}}</p>
  </div>

  {{#each reportContent}}
    <h2 class="section-title">{{this.title}}</h2>
    <pre class="section-content">{{this.content}}</pre>
  {{/each}}

  <footer>
    Rapport Thérapeutique - {{patientName}}
  </footer>
</body>
</html>
